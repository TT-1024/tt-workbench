# TT 工作台协作规则

## 默认发布到手机端

- 用户要求修改本项目时，完成实现并进行与改动风险相称的验证。
- 验证通过后，默认将本次相关改动提交到当前分支并推送到 GitHub，使 GitHub Pages 和手机端能够获取最新版；无需用户每次重复说明“推送到手机”。
- 提交前只包含本次任务相关文件，不得擅自提交用户的其他未完成改动。
- 如果用户明确说“不要推送”“只改本地”“先给我看看”或类似表述，则不要提交或推送。
- 如果推送失败，保留本地改动并明确告知用户失败原因和当前状态，不得声称手机端已经更新。

## Web App 缓存更新

- 修改会影响手机端显示或行为的静态资源时，检查 Service Worker 缓存策略。
- 必要时更新 `sw.js` 中的缓存版本，并更新 `index.html` 中相关资源的版本参数，避免主屏幕 Web App 长期使用旧文件。
- 发布后说明手机端何时会自动更新；若需要关闭并重新打开 App，也要明确提示。
